<?xml version="1.0" encoding="UTF-8"?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:d="http://xamarin.com/schemas/2014/forms/design"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Class="WhitePaperBible.Views.PapersPage"
    Title="Papers">
    <!--<ContentPage.ToolbarItems>
        <ToolbarItem Icon="search.png" Clicked="Handle_Clicked"/>
    </ContentPage.ToolbarItems>-->
    <CollectionView
        x:Name="cv"
        ItemsSource="{Binding Papers}"
        SelectedItem="{Binding SelectedPaper, Mode=TwoWay}"
        SelectionMode="Single"
        SelectionChangedCommand="{Binding PaperSelectedCommand}">
        <CollectionView.ItemsLayout>
            <GridItemsLayout
                Orientation="Vertical"
                Span="1" />
        </CollectionView.ItemsLayout>
        <d:CollectionView.ItemsSource>
            <x:Array
                Type="{x:Type x:String}">
                <x:String>Lorum Ipsum</x:String>
                <x:String>Your feedback on the experience of converting a ListView to a CollectionView is incredibly appreciated.</x:String>
                <x:String>Here are three general questions:</x:String>
                <x:String>How was the experience of converting your existing ListView to a CollectionView?</x:String>
                <x:String>How is the performance compared to the ListView?</x:String>
                <x:String>Is there a specific piece of functionality that you'd like to see?</x:String>
                <x:String>This is a CollectionView!</x:String>
                <x:String>Your feedback on the experience of converting a ListView to a CollectionView is incredibly appreciated.</x:String>
                <x:String>Here are three general questions:</x:String>
                <x:String>How was the experience of converting your existing ListView to a CollectionView?</x:String>
                <x:String>How is the performance compared to the ListView?</x:String>
                <x:String>Is there a specific piece of functionality that you'd like to see?</x:String>
                <x:String>This is a CollectionView!</x:String>
                <x:String>Your feedback on the experience of converting a ListView to a CollectionView is incredibly appreciated.</x:String>
                <x:String>Here are three general questions:</x:String>
                <x:String>How was the experience of converting your existing ListView to a CollectionView?</x:String>
                <x:String>How is the performance compared to the ListView?</x:String>
                <x:String>Is there a specific piece of functionality that you'd like to see?</x:String>
            </x:Array>
        </d:CollectionView.ItemsSource>
        <CollectionView.EmptyView>
            <StackLayout>
                <Label
                    HorizontalOptions="CenterAndExpand"
                    VerticalOptions="CenterAndExpand"
                    Text="Loading Papers" />
            </StackLayout>
        </CollectionView.EmptyView>
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <ContentView
                    Padding="10">
                    <Frame
                        HasShadow="false"
                        BackgroundColor="#f1f1f1"
                        OutlineColor="#f2f2f2"
                        Padding="15,8"
                        CornerRadius="4">
                        <StackLayout
                            Padding="0">
                            <Label
                                FontSize="15"
                                MaxLines="1"
                                TextColor="Black"
                                d:Text="{Binding .}"
                                Text="{Binding title}" />
                            <Label
                                FontSize="11"
                                TextColor="Gray"
                                d:Text="Sally Someone"
                                Text="{Binding Author.Name}" />
                        </StackLayout>
                    </Frame>
                </ContentView>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>