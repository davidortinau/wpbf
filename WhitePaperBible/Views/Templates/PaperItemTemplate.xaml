<?xml version="1.0" encoding="UTF-8"?>
<ContentView
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="WhitePaperBible.Views.Templates.PaperItemTemplate">
    
    <Frame
        HasShadow="false"
        BackgroundColor="{AppThemeBinding Dark={StaticResource NeutralDark}, Light={StaticResource TabColor_Light}}"
        
        Padding="15,8"
        CornerRadius="4">
        <StackLayout
            Padding="0">
            <Label
                FontSize="15"
                MaxLines="1"
                TextColor="{AppThemeBinding Dark={StaticResource TextPrimaryColor_Dark}, Light={StaticResource TextPrimaryColor_Light}}"
                Text="{Binding title}" />
            <Label
                FontSize="11"
                TextColor="{AppThemeBinding Dark={StaticResource TextSecondaryColor_Dark}, Light={StaticResource TextSecondaryColor_Light}}"
                Text="{Binding Author.Name}" />
        </StackLayout>
    </Frame>
                    
</ContentView>
